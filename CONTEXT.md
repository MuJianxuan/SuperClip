# SuperClip Product Surfaces

macOS 菜单栏优先的剪贴板历史工具。本上下文记录产品表面与交互职责的 ubiquitous language，不含实现细节。

## Language

### Product posture

**菜单栏优先（Menu-bar first）**:
产品的日常入口是菜单栏与全局快捷键，而不是常驻大窗口应用。Dock 不作为主入口。
_Avoid_: 主窗口优先、常驻工作区 App

**柔和消费级视觉（Soft consumer visual language）**:
界面气质偏大圆角、轻色彩点缀、更松但仍克制的层级，亲和、年轻，而不是极客冷感或纯系统原生仿造。
_Avoid_: Linear 极客风、纯换皮、花哨品牌展柜

**中等密度（Medium density）**:
在柔和消费级视觉下仍保持可扫读的信息量；Popup 以大约一屏 7–10 条历史为密度目标。
_Avoid_: 纯松散展览式、仅换皮的偏紧旧密度

### Surfaces

**Popup**:
由 Tray 单击或全局快捷键唤起的历史列表面板；内部仅为搜索 + 可滚动列表，并支持悬停 Preview。无功能底栏、无类型轻过滤。
_Avoid_: 主窗口、菜单、弹窗（含糊统称）、快捷控制面板、带底栏的旧 Popup

**快捷控制面板（Quick Control Panel）**:
由 Tray 右键唤起的少量高频控制表面，固定为：暂停/恢复监听、默认粘贴模式、打开 Main、打开 Settings、退出。不是完整 Settings，也不是历史列表。
_Avoid_: Tray Menu（旧称）、双击面板、迷你设置页、Popup

**Main**:
偶尔使用的管理台，承载完整浏览、过滤、批量操作与完整设置。
_Avoid_: 主 App、设置窗、常驻工作区

**Tray 交互**:
单击打开 Popup；右键打开快捷控制面板。不以双击作为主发现路径。
_Avoid_: 双击打开功能面板、左键菜单右键历史

**Preview**:
悬停 Popup 历史行后，在列表旁出现的独立预览浮窗；只读、不抢点击主路径。视觉纳入全表面统一设计，不改为 Popup 内嵌卡片。
_Avoid_: Popup 内嵌预览卡、仅富内容才预览（未采纳）、Popover（旧 DOM 实现名）

**Settings**:
完整偏好与规则配置，属于 Main 内的现代分区/分栏表面，不再是盖死历史的临时全屏模式。
_Avoid_: 设置弹窗、全屏盖层、独立设置窗（已否决）

**历史保留上限（History limit）**:
用户可配置的剪贴板历史存档条数上限；Popup 与 Main 在该上限内浏览同一批历史，不另设「仅最近 100 条」的 Popup 窗口。
_Avoid_: Popup 首屏条数、分页加载更多（已否决为此次模型）

**Popup 列表滚动**:
Popup 高度固定，列表在面板内虚拟滚动；鼠标滚轮与触控板双指滚动作用于列表，用户可滚到历史上限内的更早记录。
_Avoid_: 滚动整窗位移、仅显示固定 N 条不可继续滚

### Redesign scope

**全表面统一重设计**:
Popup、Main、Settings、Preview 与快捷控制面板一次采用同一套柔和消费级视觉与交互语言。

**交互级重设计（IA + UX redesign）**:
不只更换视觉，也重排信息架构与关键交互流程，包括入口职责、Settings 呈现、Preview 与批量操作等。
_Avoid_: 仅换皮、只改 CSS

---

## Confirmed design (2026-08-03)

五个表面的交互级重设计已全部完成并在 `prototype.html`（Variant B/D/E/F/G）中确认。以下为开发基准；原型文件位于 `src/prototype/variants/`，作为最终视觉参考。

### 统一视觉语言（全表面通用）

**窗口材质（Dark frosted glass）**:
深色磨砂玻璃：背景 `rgba(16-18, 21-23, 28-30, 0.78-0.88)` + `backdrop-filter: blur(36-40px) saturate(1.6)`；边框 `rgba(255,255,255,0.06)`；外阴影 `0 24-32px 60-100px rgba(0,0,0,0.4-0.45)` + 内描边 `0.5px rgba(255,255,255,0.04)`。

**圆角**:
Popup/面板/浮窗 12-14px；Main 与 Settings 窗口 18px。

**色彩 token**:
- 强调蓝 `#38bdf8`（选中/焦点/主操作）
- 置顶琥珀 `#fbbf24`（pinned，与强调蓝区分）
- 危险红 `#ef4444`（删除/退出）
- 类型色：text `#38bdf8`、image `#fb7185`、file `#60a5fa`、html `#a78bfa`（全表面统一）

**选中态**:
浅蓝底 `rgba(56,189,248,0.07)` + 左侧 3px 类型色条 + 图标染色 + 文字提亮；hover 底色 `rgba(255,255,255,0.03)`。

**交互实现约束**:
hover/active 一律由 React state 驱动，禁止直接操作 `e.currentTarget.style`（会与 React 声明式样式冲突，造成背景滞留）。

**进入动画**:
Popup 行滑动 `rowSlideIn`；Quick Panel `panelPopIn`（scale 0.96 + fade）；Preview `previewFadeIn`（fade + 4px 位移）；Settings 分区切换 `fadeSlideIn`。

### Popup（B2）

**结构**:
单行紧凑搜索框（搜索图标 + 输入 + 清空 × + `⌘⇧V` 快捷键徽标，hover 边框泛蓝）+ 可滚动列表 + 单行信息底栏（无任何按钮）。无独立标题行。

**行**:
两行结构：标题 13px（单行截断）+ 预览 11px（单行截断）；左侧 28px 类型图标；右侧时间 10px + 置顶琥珀星；行高 ≈46px，一屏 8-9 条。

**悬停 Preview**:
悬停行 200ms 后，在 Popup 右侧弹出独立浮窗（对齐该行、空间不足切左侧、clamp 到视口内）；移入浮窗有 150ms 容错。文本 280px / 图片 240px 宽。

**空态**:
搜索无结果显示「没有匹配的内容」；无记录显示「剪贴板暂无记录」。

### Quick Control Panel（D2）

**结构**:
宽度 232px，圆角 12；Header（品牌 + 实时状态胶囊：绿点「监听中」/ 橘点「已暂停」）+ 分组列表：监听控制 → 粘贴模式 → 打开 Main/设置 → 退出。

**交互**:
- 暂停/恢复：图标语义 = 监听中显示暂停符号、已暂停显示播放符号；按钮切换状态时文案/图标/胶囊联动
- 粘贴模式：分段控件（直接粘贴 / 仅复制），选中蓝底蓝边，下方单行说明
- 打开 Main/设置：带快捷键徽标（⌘M / ⌘,）
- 退出：danger 样式，hover 红底红字提亮

### Main（E2）

**窗口**:
760×540，圆角 18px。

**工具条**:
单行：identity（渐变图标 + 名称）+ 主导搜索框（带清空 ×）+ 视图切换（列表/网格分段）+ 设置按钮。无独立标题行、无三行堆叠。

**过滤 chips**:
药丸形带计数徽标；选中蓝底蓝边（置顶 chip 用琥珀）；一行横滑。

**列表**:
按「今天 / 昨天 / 更早」日期分组；行 = 类型色点 + 标题 + 预览 + 应用 + 时间 + 置顶星；hover 显示行操作（复制/置顶/删除）；选中 = 勾选框 + 浅蓝底。

**批量操作栏**:
有选中时从底部滑入（slide-in）：全选/清空选择 + 计数（蓝色高亮）+ 复制/置顶/删除批量按钮。

**空态**:
区分「搜索无结果」与「无记录」两种，带清空搜索按钮。

**网格视图**:
卡片 `minmax(160px, 1fr)`，顶部类型色渐变条，20px 图标。

**保留槽位**:
工具条与列表之间预留状态横幅位（用于权限未授权 / 仅复制模式等真实状态）。

### Settings（F2，作为 Main 分区）

**呈现**:
复用 Main 窗口框架（760×540@18px）与 toolbar（identity + 返回列表按钮），左侧分区导航 + 右侧内容区。

**导航**:
通用 / 快捷键 / 排除规则 / 高级；选中 = 浅蓝底 + 左侧 3px 蓝色条 + 蓝字。

**内容区**:
每个设置组为浅底圆角卡片；分区切换带 fadeSlideIn 动画。

**控件**:
- Toggle：真实 macOS 式开关（38×22 轨道 + 白色圆钮 + 弹性动画 + 开启蓝 glow），不用图标代替
- 滑块：渐变填充轨道（填充比例随值变化）+ 白色圆钮 + hover 放大；数值蓝色高亮
- 分段按钮（主题）：同 Quick Panel 语言

**危险操作**:
导出诊断数据：红底按钮，hover 加深。

### Preview（G2）

**只读**:
浮窗内无任何操作按钮（无复制/查看/关闭）——符合「只读、不抢点击主路径」。

**结构**:
宽度 320；Header = 类型色点 + 类型标签 + 时间；内容区（文本/图片自适应）；底部一行「来自 · App」。

**富内容渲染**:
文本区分普通段落与代码块（``` 包裹段 → 等宽深底块，剥离首行语言标注，类型色文字）。

**图片占位**:
类型色渐变底 + 径向光斑装饰（原型阶段占位，开发时替换真实缩略图）。
