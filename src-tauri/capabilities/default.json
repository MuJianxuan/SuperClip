{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all application windows",
  "windows": ["main", "popup", "preview"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
