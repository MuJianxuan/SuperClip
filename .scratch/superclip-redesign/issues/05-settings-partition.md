# 05 — Settings 分区化（F2）

**What to build:** 设置从全屏盖层改为 Main 内的分区/分栏视图（不再是盖死历史的临时全屏模式）：
- 工具条切换为「返回列表」按钮 + 设置身份
- 左侧分区导航：通用 / 快捷键 / 排除规则 / 高级，选中 = 浅蓝底 + 左侧 3px 色条
- 分区内容卡片化：主题分段选择、历史保留上限滑块（数值蓝色高亮）、启动行为真实 macOS 式开关（轨道 + 圆钮弹性动画）、快捷键行（绑定徽标 + 更改）、排除规则增删、存储信息、导出诊断（danger 红色 hover）
- 分区切换带淡入动画
- 数据层复用现有设置 / 规则 / 快捷键命令，改动即时持久化

**Blocked by:** 04 — Main 重构（E2，提供窗口框架与工具条）

**Status:** done (committed ce573ae + 3bc5bc4)

- [ ] 视觉与 F2 原型一致；不再有全屏 overlay 盖层
- [ ] 四个分区可切换且动画生效，「返回列表」恢复剪贴板视图
- [ ] 主题 / 历史上限 / 启动行为 / 快捷键 / 规则各设置写入生效（重启后保留）
- [ ] 排除规则增删与诊断导出可用，危险操作 hover 红化
- [ ] Settings 相关测试更新并通过
